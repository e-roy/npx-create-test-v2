{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspace @create-dao/next-app dev",
    "start": "yarn workspace npx-create-test-v2 start",
    "publish-stable": "lerna version --force-publish --no-private"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "packages/next-test-v2/templates/*"
  ]
}
