{
  "name": "root",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspace @create-dao/next-app dev",
    "start": "yarn workspace npx-create-test-v2 start",
    "update-version": "lerna version --force-publish --no-private",
    "publish-latest": "leran publish from-package"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*",
    "packages/next-test-v2/templates/*"
  ]
}
